# ============================================================
# 答案之书 · 大学生专供版
# ============================================================

# ---------- 开发侧文档与记忆（不发布） ----------

# 设计与决策文档：写给维护者看的，含本机路径、踩坑记录、内部取舍
PLAN.md

# WorkBuddy 的工作记忆目录
.workbuddy/

# ---------- 开发侧截图与取证脚本（不发布） ----------

# 效果预览图、前后对照图、动画冻结帧
screenshots/

# 取证脚本工作目录（逐帧探针、参数扫描等一次性工具，产物写进 screenshots/）
.shots/

# ---------- 依赖 ----------
# 注意：package-lock.json 不在忽略之列。
# 它是测试依赖的唯一版本凭据，提交上去别人才能复现出同版本的 playwright-core。
node_modules/

# ---------- Python ----------
# tools/build_data.py 是 Python 脚本。直接运行它不会产生缓存，
# 但一旦有人 import 它（或跑 py_compile），Python 就会写出这些目录。
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/

# ---------- 编辑器 / IDE ----------
.vscode/
.idea/
*.swp
*.swo
*~
.project
.settings/

# ---------- 操作系统 ----------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ---------- 临时文件与日志 ----------
*.log
*.tmp
*.temp
/tmp/
.cache/
